{{define "adminlogin"}}{{template "adminheader" .}}
</head>

<body>
    <div id="cont" class="units-container bckgrnd">
        {{template "admintop" .}}



        <div id="main" class="units-row end" style="padding-top:70px;">
            <div class="units-centered unit-80 bckgrnd5" style="padding:15px 20px;">
                <div class="units-row">
                    <div class="unit-60">
                        <form method="post" action="/form/login" class="forms">
                            <label>Login
                            <input type="text" name="Login" class="width-100" /></label>
                            <label>Hasło
                            <input type="password" name="Password" class="width-100" /></label>
                            <button class="btn btn-blue" type="submit">Zaloguj</button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
        {{template "adminfooter" .}} {{end}}
